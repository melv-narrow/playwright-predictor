{
  "language": "javascript",
  "code": "import { test, expect } from '@playwright/test';  \n  \ntest.beforeAll(async () => {  \n  // Set timeout for this hook.  \n  test.setTimeout(60000);  \n});",
  "context": "`beforeAll` and `afterAll` hooks have a separate timeout, by default equal to test timeout. You can change it separately for each hook by calling [testInfo.setTimeout()](/docs/api/class-testinfo#test-info-set-timeout) inside the hook. | example.spec.ts",
  "source_url": "https://playwright.dev/docs/test-timeouts",
  "pattern": "fixture"
}